import DashboardPageClient from './components/DashboardPageClient';

export const metadata = { title: 'Dashboard — TreasuryDesk' };

export default function DashboardPage() {
  return <DashboardPageClient />;
}
